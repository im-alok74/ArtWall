import { ArrowRight, Plus } from "lucide-react";
import { StudioButton, StudioEmptyState, StudioPageHeader } from "@/components/dashboard/studio-shell";
export default function SalesPage() { return <div className="flex flex-col gap-8"><StudioPageHeader eyebrow="Business" title="Sales pipeline" description="See interest become conversation, and conversation become the right next home for a work." action={<StudioButton><Plus data-icon="inline-start" />Add opportunity</StudioButton>} /><div className="studio-card"><StudioEmptyState title="Your pipeline is clear" description="When a collector asks about a work, add the opportunity here to keep the next step visible." action={<StudioButton><ArrowRight data-icon="inline-start" />Add first opportunity</StudioButton>} /></div></div>; }
