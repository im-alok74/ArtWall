import { StudioEmptyState, StudioPageHeader, StudioButton } from "@/components/dashboard/studio-shell";
export default function PaymentsPage() { return <div className="flex flex-col gap-8"><StudioPageHeader eyebrow="Business" title="Payments" description="Track payment status alongside each sale without processing money in the archive." action={<StudioButton>Record payment</StudioButton>} /><div className="studio-card"><StudioEmptyState title="No payments yet" description="Payment tracking will use server-side records and never expose sensitive credentials." /></div></div>; }
