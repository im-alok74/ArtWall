import { ClipboardList, Plus } from "lucide-react";
import { StudioButton, StudioEmptyState, StudioPageHeader } from "@/components/dashboard/studio-shell";
export default function TasksPage() { return <div className="flex flex-col gap-8"><StudioPageHeader eyebrow="Operations" title="Tasks" description="Keep the small, consequential work of a practice moving." action={<StudioButton><Plus data-icon="inline-start" />Add task</StudioButton>} /><div className="studio-card"><StudioEmptyState title="No open tasks" description="Add a task when something deserves a place outside your head." action={<StudioButton><ClipboardList data-icon="inline-start" />Add first task</StudioButton>} /></div></div>; }
