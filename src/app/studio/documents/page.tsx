import { FileText, Plus } from "lucide-react";
import { getDocuments } from "@/app/actions/workspaces";
import { StudioButton, StudioEmptyState, StudioPageHeader } from "@/components/dashboard/studio-shell";
export default async function DocumentsPage() { const items = await getDocuments(); return <div className="flex flex-col gap-8"><StudioPageHeader eyebrow="Archive" title="Documents" description="Keep certificates, invoices, agreements, and the supporting record close to each work." action={<StudioButton><Plus data-icon="inline-start" />Upload document</StudioButton>} />{items.length === 0 ? <div className="studio-card"><StudioEmptyState title="No documents yet" description="Documents added to this workspace will be organized and permissioned here." action={<StudioButton><FileText data-icon="inline-start" />Add first document</StudioButton>} /></div> : <div className="studio-card flex flex-col divide-y divide-studio-border">{items.map((item) => <div key={item.id} className="flex items-center justify-between p-5"><p className="text-sm font-medium text-studio-ink">{item.title}</p><span className="studio-eyebrow">{item.kind}</span></div>)}</div>}</div>; }
