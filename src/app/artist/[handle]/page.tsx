import { notFound } from "next/navigation";
export default async function ArtistProfilePage({ params }: { params: Promise<{ handle: string }> }) { const { handle } = await params; if (!handle) notFound(); return <main className="mx-auto flex min-h-screen max-w-5xl flex-col gap-12 px-6 py-16"><p className="studio-eyebrow">Artist profile</p><h1 className="font-serif text-6xl">{handle}</h1><p className="max-w-2xl text-lg leading-8 text-muted-foreground">A considered public profile for an ArtWall artist, with biography, featured works, collections, and contact details.</p></main>; }
